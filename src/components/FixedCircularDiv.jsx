

export default function Contact() {
  return (
    <div className="flex items-center justify-center space-x-8 py-24 h-screen">
      {/* Fixed size circular div */}
      <div className="relative flex items-center justify-center w-80 h-80 rounded-full bg-purple-50 p-5 text-center shadow-xl">
    
        Make it simple, but significant.
      </div>
    </div>
  );
}